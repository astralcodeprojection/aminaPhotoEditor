import React from 'react';
import { Box } from '@mui/material';
import LoadPhoto from './LoadPhoto'; // Import the LoadPhoto component

const Tab3 = () => {
  // You can keep any specific logic related to Tab1 here if needed

  return (
    <Box>
      <LoadPhoto /> {/* Render the LoadPhoto component */}
    </Box>
  );
};

export default Tab3;